import { DefaultTheme } from "styled-components";
import oc from "open-color";

const theme: DefaultTheme = {
  primaryRed: `#ea443a`,
  blackColor: oc.gray[9],
  grayColor: oc.gray[6],
  borderColor: "#e5e3dd",
  subtitle: "16px",
  caption: "14px"
};

export default theme;
